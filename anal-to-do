#### batch debates
// H3 & H5 - ABM vs LINEAR REGRESSION COMPARISON
// fit OLS using only initial_opinion as predictor of final_attitude
// compute regression MAE per debate
// compare regression MAE vs ABM MAE per debate and per model type
// test significance with paired wilcoxon test
// plot ABM MAE vs regression MAE scatter with debate_label as identifier

// H4 - HOMOGENEOUS VS HETEROGENEOUS DEBATE COMPOSITION
// filter by debate_label prefix (H vs M)
// compare mean absolute opinion change between H and M debates
// test with wilcoxon or t-test
//\\ NOTE: rename use_heterogeneous_agents references to use_distinct_agents

// SPEAKING MODE COMPARISON (new)
// filter speaking_mode = true vs false
// compare MAE distribution per model type
// compare convergence_cycle speaking vs non speaking
// normalise convergence_cycle by max_cycles for comparability
// test statistical significance

// EXPERIMENT_ID FILTERING
// add experiment_id as primary grouping variable
// replace complex multi-column filtering with single experiment_id filter
// update boss_table and model_comparison to group by experiment_id

// NO_CHANGE BASELINE (new)
// load no_change results separately
// compute baseline MAE per debate
// compute delta_MAE = model_MAE - baseline_MAE per debate
// negative delta = model improves on baseline
// add to boss_table and model_comparison

// UPDATE COLUMN NAMES
// rename use_heterogeneous_agents to use_distinct_agents throughout
// add debate_label based H/M filtering to existing analyses
// remove homophily_strength references (no longer a parameter)


#### Additions for agent-level-analyses.R:
// H1 - PRE TO POST DELIBERATION ATTITUDE PREDICTION
// regress final_attitude on initial_opinion per debate_label group
// compute R squared per model type and condition
// compare explained variance across H and M debates

// H2 - SOCIAL NORMS AND SELF CONTROL (requires merging with original data)
// merge df_ag with df_orig on agent_id
// regress abs opinion change on perceived_norms + self_control
// add initial_opinion as moderator
// test interaction term initial_opinion * perceived_norms

// SPEAKING TURN ANALYSIS (new - requires recent_speech_str)
// parse recent_speech_str using strsplit(recent_speech_str, ";")
// reconstruct speaking order per debate per seed
// compute speaking_position = which turn agent spoke
// correlate speaking_position with opinion_change
// identify first mover advantage
// test whether agents who speak more change more

// UPDATE COLUMN NAMES
// rename use_heterogeneous_agents to use_distinct_agents
// add experiment_id as grouping variable
// add debate_label for H/M filtering
